# 电摩情报站

一个电动摩托车信息聚合网站前端原型，聚合车型参数、资讯流、补能指数、热度趋势和快速对比功能。

## 运行方式

直接打开 `index.html` 即可预览。

## 文件结构

- `index.html`：页面结构
- `styles.css`：响应式布局与视觉样式
- `app.js`：样例数据、筛选、对比和资讯交互逻辑

## 后续接入

当前数据位于 `app.js` 的数组中，可替换为真实 API、CMS、爬虫结果或静态 JSON 数据源。
